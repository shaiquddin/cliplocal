# Use an existing directory containing media you own or are authorized to process.
# Windows paths should use forward slashes, for example: E:/Videos
MEDIA_DIR=./media

# Additional local hostnames accepted by ClipLocal (comma-separated).
APP_ALLOWED_HOSTS=cliplocal.dev
